<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Mostrar.mostrarInfoBit"></activity>
        <activity android:name=".Mostrar.mostrarInfoPersona" />
        <activity android:name=".bitac.setBitacora" />
        <activity android:name=".inserts.setInfoPersona" />
        <activity android:name=".MenuIncio" />
        <activity android:name=".inserts.updateSetPersona" />
        <activity android:name=".inserts.setPersona" />
        <activity android:name=".comparaFecha" />
        <activity android:name=".pantallaOpcion" />
        <activity android:name=".Activity_sing_up" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>